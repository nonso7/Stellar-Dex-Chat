{
  "scripts": {
    "prepare": "husky",
    "precommit:clippy": "cd stellar-contracts && cargo clippy --all-targets --all-features -- -D warnings",
    "precommit:eslint": "npm --prefix dex_with_fiat_frontend run lint --"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.6.1"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  }
}
